---
import type { HTMLAttributes } from "astro/types"

interface Props extends HTMLAttributes<"a"> {
  href: string
}

const { href, ...rest } = Astro.props

if (!Astro.slots.has("default")) {
  throw new Error("<SiteFooterLink> requires content.")
}
---

<a
  href={href}
  class="font-mono text-2xs text-muted-foreground transition-colors hover:text-primary"
  {...rest}
>
  <slot />
</a>

---
interface Props {
  stem: string
  tails: string[]
}

const { stem, tails } = Astro.props
---

<p
  data-hero-badge
  class="group flex items-center gap-2 font-mono text-badge text-muted-foreground lowercase"
>
  <span
    data-badge-spinner
    aria-hidden="true"
    class="inline-block w-[1ch] text-primary"></span>
  <span>
    {stem}{" "}
    {
      tails.map((tail: string, index: number) => (
        <span
          data-badge-tail
          hidden={index > 0}
          aria-hidden={index > 0 ? "true" : undefined}
        >
          {tail}
        </span>
      ))
    }
  </span>
  <span
    aria-hidden="true"
    class="inline-block h-3.75 w-1.75 bg-foreground group-data-typing:animate-none group-data-typing:opacity-100 motion-safe:animate-blink"
  ></span>
</p>

<script>
  import "@/scripts/hero-badge"
</script>
